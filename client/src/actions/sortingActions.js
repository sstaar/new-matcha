export const sortAge = (order, type) => {

	return {
		type,
		payload: order
	}
}

export const sortDistance = (order, type) => {

	return {
		type,
		payload: order
	}
}

export const sortFame = (order, type) => {

	return {
		type,
		payload: order
	}
}